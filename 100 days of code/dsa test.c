#include <stdio.h>
#include <stdlib.h>
#define SIZE 10;
struct complain {
    int sap id;
    int room number;
    int complain type;
    struct complain* next;
}
int malloc[struct complain ; int sapid ; char (type)]{
};

 
